var Calendar = Calendar || {};
Calendar.EventItem = Backbone.Model.extend({
    defaults : {
        maxWidth: 600,
        hourMaping: 29*2 // this is the pixels for 1 hour
    },
    initialize: function function_name (argument) {
        // body...
    }
});